<%= form_for @agente do |f| %>
  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :agen_nombre %>
  </div>

  <div class="actions">
    <%= f.submit "Guardar cambios" %>
  </div>
<% end %>
