<!-- app/views/agentes/new.html.erb -->
<h1>Nuevo Proveedor</h1>

<%= form_with(model: @agente, local: true) do |form| %>
  <% if @agente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@agente.errors.count, "error") %> prohibió que este agente se guardara:</h2>

      <ul>
        <% @agente.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :agen_nombre, id: :agente_nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= link_to 'Volver',agentes_path%>